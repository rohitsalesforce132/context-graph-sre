# Precedent Chain: Disk Pressure Response Pattern

```yaml
id: PRE-001
type: precedent_chain
title: "Node NotReady + Disk Pressure: Skip Container Runtime Restart"

pattern: |
  When Kubernetes nodes show NotReady status AND disk pressure condition,
  clean disk first. Do NOT restart container runtime as first action.

evolution:
  - date: "2026-01-15"
    incident: INC-EARLIER-01
    decision: DEC-EARLIER-01
    action: "Followed runbook — restarted runtime first"
    outcome: "Extended outage. Cascading pod evictions across nodes."
    lesson: "Runtime restart on disk-pressure nodes is dangerous"
    mttr: 35

  - date: "2026-04-12"
    incident: INC-001
    decision: DEC-001
    action: "Skipped runtime restart, disk cleanup only"
    outcome: "Resolved in 12 min. Zero pod evictions. Clean recovery."
    lesson: "Disk cleanup alone is sufficient. Runtime restart is last resort."
    mttr: 12

recommendation: |
  For Kubernetes node NotReady + disk pressure:
  1. Confirm DiskPressure: kubectl describe node <node> | grep -A5 Conditions
  2. Check disk: ssh <node> "df -h /var/lib/kubelet /var/lib/containerd"
  3. Clean images: crictl rmi --prune  (or docker image prune -a)
  4. Clean logs: journalctl --vacuum-time=2d
  5. Wait 2 min, check node status
  6. ONLY restart runtime if STILL NotReady after cleanup

confidence: high
based_on_incidents: 2
success_rate: "100% (2/2 without runtime restart)"

tags: [kubernetes, disk-pressure, runbook-override, precedent]
```
