# Decision: Skipped Container Runtime Restart, Disk Cleanup First

```yaml
id: DEC-001
type: decision_trace
title: "Skipped runbook step 2 (container runtime restart), disk cleanup first"
timestamp: "2026-04-12T14:28:00+05:30"
incident_ref: INC-001

decision: "Skip runbook step 2 (restart container runtime), go to disk cleanup"
rationale: "Container runtime restart on disk-pressure nodes can cause cascading pod evictions."

options_considered:
  - option: "Follow runbook: restart container runtime first"
    chosen: false
    rejected_because: "Risk of cascading pod evictions under disk pressure"
  - option: "Disk cleanup first, skip runtime restart"
    chosen: true
    confidence: high
  - option: "Cordon + drain nodes, full cleanup + restart"
    chosen: false
    rejected_because: "Too slow during P1, draining 3 nodes extends outage 10+ min"

exceptions:
  - rule: "Runbook step 2: restart container runtime on NotReady"
    deviation: "Skipped step entirely, went to disk cleanup"
    justification: "Disk cleanup alone should resolve disk pressure without pod disruption"
    approved_by: "on-call judgment (standard SRE autonomy for P1)"

precedents: []

organizational_state:
  other_active_incidents: 0
  team_availability: "Full team online"
  customer_impact: "API latency > 500ms, some 502s"
  sla_breach_risk: "High"

outcome:
  result: "All 3 nodes recovered in 7 minutes. Zero pod evictions."
  mttr_minutes: 12
  mttr_vs_average: "66% faster than standard runbook"
  side_effects: "None"
  follow_up: "Update runbook: check disk pressure first"

actor:
  name: engineer-a
  role: on_call
  experience_level: "Experienced with Kubernetes node issues"

ai_reproducible: true
ai_reproducible_notes: "Circuit breaker / disk cleanup pattern is well-documented in SRE literature."

tags: [kubernetes, disk-pressure, runbook-exception, fast-resolution, precedent-setting]
```

## Why This Matters

Without a context graph, a new engineer (or AI agent) would follow the runbook and restart the container runtime — potentially causing cascading pod evictions and extending the outage.

With the context graph, they'd find DEC-001 and know to skip that step.
