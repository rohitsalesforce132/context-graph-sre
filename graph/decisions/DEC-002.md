# Decision: Circuit Breaker Instead of Timeout Increase

```yaml
id: DEC-002
type: decision_trace
title: "Added circuit breaker with 10s timeout, fail fast instead of 30s"
timestamp: "2026-04-14T09:22:00+05:30"
incident_ref: INC-002

decision: "Override provider timeout from 30s to 10s via app config, add circuit breaker"
rationale: "Provider cert expiry is their problem. Our API shouldn't hang for 30s consuming resources."

options_considered:
  - option: "Increase timeout from 30s to 60s"
    chosen: false
    rejected_because: "Just delays the 502, risks thread pool exhaustion"
  - option: "Circuit breaker with 10s, fail fast with error"
    chosen: true
    confidence: high
  - option: "Disable provider entirely until they fix cert"
    chosen: false
    rejected_because: "Too aggressive, some requests still succeed within 5s"

exceptions:
  - rule: "Provider timeout configured at 30s in infrastructure module"
    deviation: "Overriding to 10s via application config"
    justification: "Circuit breaker is standard resilience pattern. IaC update will follow."
    approved_by: "on-call judgment (P2). Retro in standup."

precedents: []

organizational_state:
  other_active_incidents: 0
  team_availability: "Full team"
  customer_impact: "15% of payment traffic affected"
  sla_breach_risk: medium

outcome:
  result: "502 rate dropped to 0% in 13 minutes. Other providers unaffected."
  mttr_minutes: 13
  mttr_vs_average: "50% faster than previous similar incident"
  side_effects: "Requests in 10-30s window now fail — <2% of traffic. Acceptable."
  follow_up: "Update IaC for per-provider timeout config"

actor:
  name: engineer-a
  role: on_call
  experience_level: "Familiar with provider integration patterns"

ai_reproducible: true
ai_reproducible_notes: "Circuit breaker is standard SRE pattern. Agent with resilience knowledge could make same call."

tags: [circuit-breaker, provider-timeout, config-override, precedent-setting]
```
